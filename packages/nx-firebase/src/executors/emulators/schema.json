{
	"$schema": "https://json-schema.org/schema",
	"version": 2,
	"title": "Emulators executor",
	"description": "",
	"type": "object",
	"properties": {
		"cwd": {
			"type": "string",
			"description": "The directory from which the emulators should be started. This should be the same directory as your firebase.json"
		}
	},
	"required": []
}
